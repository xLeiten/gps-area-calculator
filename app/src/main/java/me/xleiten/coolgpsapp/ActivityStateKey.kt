package me.xleiten.coolgpsapp

enum class ActivityStateKey(val id: String) {
    ACTION_TYPE("action_type"),
    SAVED_POINTS("saved_points"),
    AREA_TYPE("area_type"),
    LAST_LATITUDE("last_latitude"),
    LAST_LONGITUDE("last_longitude"),
}